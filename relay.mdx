---
title: "Relay: Headless Hub for Bridges and Viewers"
sidebarTitle: "Relay"
description: "The Rewire relay sits between bridges and viewers. Either side can restart without taking the other down, with automatic reconnect and latched topic replay."
---

The relay is a headless hub that sits between bridges and viewers. Bridges stream into it, viewers connect to it, and either side can restart without taking the other down. A viewer that joins mid-session goes live immediately, and the bridge replays static data and latched topics to each new reader — so a late viewer still gets its transform tree and URDF models.

You rarely have to think about it. The `rewire record` flow embeds a relay automatically. Run one explicitly with `rewire serve` when you want a long-lived hub — on a robot, a lab server, or anywhere viewers come and go.

## Embedded Relay (the Default)

When you run `rewire record` with no flags and port `9876` is free, the bridge starts an in-process relay and connects the bundled [Rewire Viewer](./viewer) through it. Closing or restarting the viewer no longer interrupts recording — reopen a viewer and it picks up the buffered stream.

When the port is already busy, the bridge probes what is listening and adapts. It uses an existing relay as-is, detects a legacy 0.5.x `rewire-viewer` via its service port, and treats anything else as a stock Rerun viewer.

A `rewire record --save` run resolves the same live connection, so an archive is written and streamed at once. Pass `--no-live` to skip the probe and the spawn entirely and write only the file.

## Standalone Relay: `rewire serve`

Start a standalone relay with:

```bash
rewire serve
```

Bridges connect to it with `--connect`, from the same machine or across the network:

```bash
rewire record --connect relay-host:9876
```

| Flag | Description | Default |
|---|---|---|
| `--bind <ADDR>` | Address to bind the gRPC listener on | `0.0.0.0` |
| `--port <PORT>` | gRPC listen port | `9876` |
| `--memory-limit <LIMIT>` | Buffered history limit before the oldest data is dropped (`64MiB`, `500MB`, `50%`, `unlimited`) | `1MiB` |
| `--cors-allowed-origin <PATTERN>` | Additional origin allowed for cross-origin gRPC-web requests (repeatable) | — |
| `--save [STEM]` | Archive everything the relay receives to a `.rrd` file | — |
| `--config <PATH>` | Load JSON5 config file (`serve` section) | `~/.config/rewire/config.json5` |

All flags can also be set in the `serve` section of the JSON5 config file; CLI flags take precedence over config file values.

## Buffered History

<Tip>
  The relay keeps essentially no temporal history at the `1MiB` default — and
  that is intentional. A backfill sent at wire speed on top of a real-time
  stream parks unread in the viewer's receive queue and shows up as staleness
  that never clears, scaling with the size of the buffer. A reconnecting viewer
  gets its scene from the bridge's latched-topic replay, not from relay history,
  so a large buffer costs latency and buys nothing. Raise `--memory-limit` only
  if you specifically want deep late-join backfill from the relay itself, and
  expect a proportional stall in any viewer that joins mid-session.
</Tip>

At the `1MiB` default, readers are served the present rather than a backfill of the past. Static and store-info records are evicted last, so the scene survives even at this size for topologies with no replaying bridge in front of the relay.

<Note>
  This behavior changed in v0.9.0. Earlier versions defaulted to `512MiB` of
  buffered history.
</Note>

## Automatic Reconnect

Bridges supervise their connection to the relay, and viewers supervise theirs. When a relay or viewer restarts, the bridge reconnects on its own and keeps streaming the same recording — no restart, no manual intervention.

Reconnecting alone is not enough for ROS 2 data. Latched topics like `/tf_static` are published once, and their publishers are often gone by the time a viewer rejoins. Rewire replays them after every reconnect, along with one-time static data such as coordinate frames and URDF models, so a restarted relay or a late viewer still gets a complete transform tree.

The same replay covers viewers that join late. A latched message published once at startup can be pushed out of the relay's buffer by hours of high-rate traffic, so the bridge watches for readers joining the relay and replays its latched cache to each of them at the original timestamps. Late joiners get their transforms and models no matter how long the session has been running.

<Warning>
  Data logged while the relay was unreachable is not re-sent to the live stream.
  For a gapless record of a session, combine `--connect` with `--save` — the
  local archive keeps writing through any outage.
</Warning>

## Archiving with `--save`

The relay can archive everything it receives into a single `.rrd` file, finalized on shutdown:

```bash
rewire serve --save flight-test        # → flight-test_<timestamp>.rrd
rewire serve --save                    # → rewire-relay_<timestamp>.rrd
```

Rewire completes the filename with the session timestamp, so restarting the relay never overwrites an earlier archive. There is no rotation or retention policy — one session produces one file. For per-recording archives written by the bridge itself, use `rewire record --save`.

## Monitoring with `rewire status`

Query any relay for its identity, uptime, buffer memory, and connected bridges:

```bash
rewire status                          # queries 127.0.0.1:9876
rewire status --connect relay-host     # or host:port
```

Each connected bridge is listed with its state, application ID, active recording ID, and how recently it was heard from. Recordings started with `rewire log` appear within a heartbeat.

## Browser Viewers

The relay speaks gRPC-web and sets CORS headers, so a web build of the Rerun viewer can connect to it directly from a browser. Same-origin and localhost work out of the box. To allow additional origins, pass `--cors-allowed-origin`:

```bash
rewire serve --cors-allowed-origin "https://*.example.com"
```

The flag is repeatable — pass it multiple times to allow more than one origin pattern.

## Multi-Bridge Topologies

A single relay can fan in any number of bridges and fan out to any number of viewers. Point every robot at the same relay and open viewers wherever you need them:

```bash
# On each robot
rewire record --connect relay-host:9876

# From anywhere on the network
rewire status --connect relay-host
```

The Topics and Nodes panels in the [Rewire Viewer](./viewer) aggregate data from all connected bridges, giving you a unified view of the entire fleet from a single viewer instance.
